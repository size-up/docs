# This workflow will do a clean installation of node dependencies, cache/restore them, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions

name: CI/CD and Release

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  build:
    name: ⚙️ Build application
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [16.x, 18.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
      - name: 🌐 Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v3
        with:
          node-version: ${{ matrix.node-version }}
      - name: 🗂 Cache "node_modules"
        id: cache
        uses: actions/cache@v3
        env:
          cache-name: cache
        with:
          path: "**/node_modules"
          key: ${{ runner.arch }}-${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.arch }}-${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.arch }}-${{ runner.os }}-build-
            ${{ runner.arch }}-${{ runner.os }}-

      - name: 📦 Install or update yarn
        run: npm install --global yarn

      - name: 📦 Install dependencies
        run: yarn install --frozen-lockfile

      - name: ⚙️ Build application
        run: yarn build

      - name: 📤 Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: build
          path: ./build

      - if: ${{ steps.cache.outputs.cache-hit == 'false' }}
        name: 🗃 List the state of node modules
        continue-on-error: true
        run: yarn list

  deploy:
    name: "🐳 Deploy application [WIP]"
    runs-on: ubuntu-latest

    needs: [build]

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3

      # - name: 🌐 Use Node.js LTS
      #   uses: actions/setup-node@v3
      #   with:
      #     node-version: lts/*
      # - name: 🗂 Cache "node_modules"
      #   id: cache
      #   uses: actions/cache@v3
      #   env:
      #     cache-name: cache
      #   with:
      #     path: "**/node_modules"
      #     key: ${{ runner.arch }}-${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/yarn.lock') }}
      #     restore-keys: |
      #       ${{ runner.arch }}-${{ runner.os }}-build-${{ env.cache-name }}-
      #       ${{ runner.arch }}-${{ runner.os }}-build-
      #       ${{ runner.arch }}-${{ runner.os }}-

      - name: 🐳 Build Docker image
        run: echo "Do something like ... docker build -t ${{ secrets.DOCKER_IMAGE_NAME }} ."
      - name: 🐳 Push Docker image to registry
        run: echo "Do something like ... docker push ${{ secrets.DOCKER_IMAGE_NAME }} ."

  release:
    name: 🔖 Release application
    runs-on: ubuntu-latest

    needs: [build]

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v3
      - name: 🌐 Use Node.js LTS
        uses: actions/setup-node@v3
        with:
          node-version: lts/*
      - name: 🗂 Cache "node_modules"
        id: cache
        uses: actions/cache@v3
        env:
          cache-name: cache
        with:
          path: "**/node_modules"
          key: ${{ runner.arch }}-${{ runner.os }}-build-${{ env.cache-name }}-${{ hashFiles('**/yarn.lock') }}
          restore-keys: |
            ${{ runner.arch }}-${{ runner.os }}-build-${{ env.cache-name }}-
            ${{ runner.arch }}-${{ runner.os }}-build-
            ${{ runner.arch }}-${{ runner.os }}-

      - name: 🔖 Release application
        env:
          GITHUB_TOKEN: ${{ secrets.GH_TOKEN }}
          # NPM_TOKEN: ${{ secrets.NPM_TOKEN }} # optional, needed to publish packages on npm
        run: npx semantic-release
